# fpga-binary-calc
A simple calculator implementation for FPGAs written in SystemVerilog. Supports basic arithmetic operations (addition, subtraction, multiplication, division). Designed to be synthesized and run on an FPGA board.
